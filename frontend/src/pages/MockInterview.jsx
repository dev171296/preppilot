/**
 * Placeholder mock-interview screen. The real version — live speech
 * via Gemini Live, transcripts, rating/feedback — is deliberately not
 * built yet (that comes after Phase 0: Tasks #9-11). This stub just
 * marks where it will live.
 */
function MockInterview() {
  return (
    <section>
      <h1>Mock Interview</h1>
      <p>This screen is a placeholder. The real mock-interview flow — live voice, transcripts, and feedback — comes after login is wired up.</p>
    </section>
  )
}

export default MockInterview
